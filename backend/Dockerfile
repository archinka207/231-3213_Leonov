# Используем базовый образ с Java 17 (или ту версию, с которой совместимо ваше приложение)
FROM openjdk:21-jdk-slim

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем собранный JAR-файл из папки target в контейнер
# ЗАМЕНИТЕ 'your-app-name.jar' НА РЕАЛЬНОЕ ИМЯ ВАШЕГО JAR-ФАЙЛА
# Например, target/lab1-0.0.1-SNAPSHOT.jar
COPY target/backend-0.0.1-SNAPSHOT.jar app.jar

# Указываем порт, который будет слушать приложение внутри контейнера
EXPOSE 8081

# Команда для запуска приложения при старте контейнера
ENTRYPOINT ["java", "-jar", "app.jar"]